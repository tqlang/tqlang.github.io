---
title: Process
---
import { Funcdoc, FuncdocParam } from '@site/src/components/docs/docs-utils';

# Std.Process

:::under-construction
:::
:::not-implemented
:::

```pseudo
namespace Std.Process
```

`Std.Process` abstracts the management of the current process,
parallel tasks and threads.

---

## Functions

<Funcdoc bookmark="getProgramArguments">
    ```tq
    @public func getProgramArguments() []string
    ```
    Returns the program's execution arguments as a slice of strings.

    <FuncdocParam type="[]string" name="!ret">The program's arguments.</FuncdocParam>
</Funcdoc>

<Funcdoc bookmark="exit">
    ```tq
    @public func exit(uint status) noreturn
    ```
    Aborts the current process execution.

    <FuncdocParam type="uint" name="status">The exit status code.</FuncdocParam>
</Funcdoc>
